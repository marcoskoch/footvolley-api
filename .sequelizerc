/* padronizar caminhos windows/linux/mac */
const { resolve } = require('path');

/* exportar um objeto */
module.exports = {
  config: resolve(__dirname, 'src','config', 'database.js'), /* exportar caminho de configuração do database */
  'models-path': resolve(__dirname, 'src','app', 'models'), /* exportar caminho de models */
  'migrations-path': resolve(__dirname, 'src','database', 'migrations'), /* exportar caminho de migrations */
  'seeders-path': resolve(__dirname, 'src','database', 'seeds'), /* exportar caminho de seeds */
}